"use client";

// ────────────────────────────────────────────────────────────────────────
// MentorResponse - שכבת התגובה האנושית של מודל F3 SELECTIVE RESPOND (פיילוט M7).
//
// הרעיון: סטטוס ותגובה אנושית הן שתי שכבות נפרדות. הסטטוס (הוי הירוק או הנורה
// הענברית, הכותרת, ההסבר) נשאר בדיוק היכן שהוא ומספר ללומד אם צדק או לא. הרכיב הזה
// מוסיף אחריו שורה אחת של מלווה אנושי: אותו מנטור, אותו גודל, אותו מיקום, בהצלחה
// ובטעות כאחת. לכן הדמות אינה יכולה לתפקד כאות שגיאה: היא לא מחליפה שום אייקון
// סטטוס, ונוכחותה אינה מלמדת דבר על התוצאה.
//
// היכן הוא מופיע: בכרטיס ההכרעה של הניחוש בלבד. מסך תוצאות המבדק מקבל את משפט
// התגובה כטקסט בלבד, בלי דמות (M7, מודל C): כרטיס התוצאה נראה זהה בכל 19 הפרקים,
// ודמות שחוזרת בו הופכת למרכיב תבנית ולא לרגע הוראה.
//
// פריסה: קו-שיער עליון במקום מסגרת נוספת, כדי לא להוסיף עוד קופסה לכרטיס שכבר
// ממוסגר. הטקסט תמיד גלוי, גם בטלפון: הוא תוכן לימודי. הדמות מוסתרת מתחת ל-sm כדי
// לשמור על מסך קטן רגוע, ואינה נושאת מידע ייחודי (alt="" ברכיב Mentor).
//
// גודל: 72px, קטן במתכוון מעדשת ה-F2 של פרק 5 (92px). F3 הוא שכבה משלימה ו-F2 הוא
// רגע הוראה נדיר, וההיררכיה הזאת היא גם מה שמונע משתי הדמויות להיראות כפולות כשהן
// נופלות באותו מסך.
//
// הקראה: הרכיב אינו מוסיף SpeakButton משלו. הצרכן מצרף את המשפט לטקסט ההקראה של
// הכרטיס שבתוכו הוא יושב, כדי שלא ייווצר דיבור כפול.
//
// אין מקף ארוך, מקף בינוני או נקודה-פסיק בטקסט עברית.
// ────────────────────────────────────────────────────────────────────────

import React from 'react';
import { Mentor, type MentorPose } from './Mentor';

/**
 * פוזת F3 היחידה, זהה בהצלחה ובטעות. מנטור מסביר שמצביע פנימה אל ההסבר: זה מה
 * ששני המשפטים עושים בפועל (הצלחה - מה לקחת מכאן, טעות - קראו שוב את מה שלמעלה).
 * פוזה אחת ולא שתיים היא החלטה מכוונת: היא מוודאת שאי אפשר להסיק את התוצאה מהדמות,
 * גם לא מהבעה או מתנוחה. הסטטוס הנראה נשאר הערוץ היחיד שאומר אם צדקתם.
 */
export const RESPOND_POSE: MentorPose = 'explain';

export const MentorResponse: React.FC<{
    /** משפט התגובה. בלי משפט אין תגובה, והרכיב לא מרנדר דבר. */
    line?: string;
    className?: string;
}> = ({ line, className = '' }) => {
    if (!line) return null;
    return (
        <div className={`mt-4 flex items-center gap-3 border-t border-white/10 pt-3.5 ${className}`}>
            <div className="hidden shrink-0 sm:block">
                <Mentor pose={RESPOND_POSE} width={72} glow={false} float={false} />
            </div>
            <p className="flex-1 text-start text-[13px] leading-relaxed text-slate-300">{line}</p>
        </div>
    );
};
