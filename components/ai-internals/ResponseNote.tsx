"use client";

// ────────────────────────────────────────────────────────────────────────
// ResponseNote - שכבת התגובה האנושית של הלומדה "מאחורי הקלעים של AI".
//
// הרעיון: סטטוס ותגובה אנושית הן שתי שכבות נפרדות. הסטטוס (הוי הירוק או הנורה
// הענברית, הכותרת, ההסבר) נשאר בדיוק היכן שהוא ומספר ללומד אם צדק או לא. הרכיב הזה
// מוסיף אחריו שורה אחת של מלווה אנושי: אותו מבנה, אותו מיקום, בהצלחה ובטעות כאחת.
// לכן התגובה אינה יכולה לתפקד כאות תוצאה: היא לא מחליפה שום אייקון סטטוס, ונוכחותה
// אינה מלמדת דבר על התוצאה.
//
// M12: הקול האנושי נשאר, הדמות ירדה. אין פורטרט מנטור בלומדה הזאת בשום רוחב מסך,
// ולכן השורה הזאת היא טקסט בלבד. מה ששמר על נוכחות אנושית לא היה הצילום אלא הניסוח
// הספציפי-לפרק, והוא נשאר כפי שהוא.
//
// M13: השם עודכן מ-MentorResponse ל-ResponseNote. הרכיב מעולם לא ידע על פוזה, על
// תמונה או על היפוך, ועכשיו גם השם אינו מזמין להחזיר אותם. אין כאן שום prop של
// דמות: לא pose, לא flip, לא width. זו פסקה, וזה כל מה שהיא יכולה להיות.
//
// היכן הוא מופיע: בכרטיס ההכרעה של הניחוש בלבד. מסך תוצאות המבדק מקבל את משפט
// התגובה כטקסט ישירות מ-AssessmentEngine: כרטיס התוצאה נראה זהה בכל 19 הפרקים.
//
// פריסה: קו-שיער עליון במקום מסגרת נוספת, כדי לא להוסיף עוד קופסה לכרטיס שכבר
// ממוסגר. הקו והדרגה הטיפוגרפית הם מה שמפריד את הקול האנושי מגוף ההסבר שמעליו.
// הטקסט תמיד גלוי, גם בטלפון: הוא תוכן לימודי.
//
// הקראה: הרכיב אינו מוסיף SpeakButton משלו. הצרכן מצרף את המשפט לטקסט ההקראה של
// הכרטיס שבתוכו הוא יושב, כדי שלא ייווצר דיבור כפול.
//
// אין מקף ארוך, מקף בינוני או נקודה-פסיק בטקסט עברית.
// ────────────────────────────────────────────────────────────────────────

import React from 'react';

export const ResponseNote: React.FC<{
    /** משפט התגובה. בלי משפט אין תגובה, והרכיב לא מרנדר דבר. */
    line?: string;
    className?: string;
}> = ({ line, className = '' }) => {
    if (!line) return null;
    return (
        <p className={`mt-4 border-t border-white/10 pt-3.5 text-start text-[13px] leading-relaxed text-slate-300 ${className}`}>
            {line}
        </p>
    );
};
