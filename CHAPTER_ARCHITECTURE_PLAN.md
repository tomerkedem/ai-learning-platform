# תוכנית ארכיטקטורת הפרקים - "מאחורי הקלעים של AI"

מסמך תכנון בלבד. לא מומשו שינויים בקוד, ב-quizData, ב-courseData, ב-mastery או ב-routing.
המסמך מיישר את 16 הפרקים אל מפת התחנות החדשה של המבוא (14 תחנות LLM + שכבת Agent, 5 מערכות).

עקרונות העדיפות שהנחו את כל ההחלטות: 1) אמת, 2) בהירות ללומד, 3) הנאה ללא-מתכנתים, 4) יישור למבוא החדש, 5) הוגנות מבחן, 6) בטיחות מימוש.

---

## A. המלצת מנהלים (Executive recommendation)

הלומדה כבר עברה רענון משמעותי וקרובה מאוד למבוא החדש. שמות הפרקים, ה-quizData וה-COURSE_SYSTEMS כבר תואמים את חלוקת 5 המערכות ואת שפת המפה. רוב הפרקים דורשים **ללא שינוי** או **שינוי קטן** של פתיח/מסגור בלבד.

**ההמלצה: Option D (היברידי).** לשמר את 16 הפרקים, את המספור, ה-routing, ה-quizData וה-mastery, ולשמר את מבנה הסיפור הקיים: מערכה 1 (פרקים 1-4) היא **מבט-על ספירלי** שפותח את המנוע מבחוץ, ואז פרקים 5-9 צוללים פנימה לפי סדר המפה, פרקים 10-13 פותחים את שכבת ה-Agent, ופרקים 14-16 מסנתזים. זהו מבנה "שלם לפני חלקים" שמתאים ללא-מתכנתים ושובר את ההצגה הליניארית של המבוא בכוונה (ולא בטעות).

**הבעיה האמיתית היחידה היא פער כיסוי, לא פער יישור:** אזור C במפה ("חישוב ההקשר": Attention, ערבוב מידע, שכבות Transformer, ייצוג פנימי עדכני) הוא האזור היחיד ללא בית פרקי ברור. Attention מובטח בולט במבוא ("מילה כמו 'הוא' יודעת למי היא מתייחסת") אך אינו נלמד היום בפרק ייעודי. פערים משניים קלים יותר: מיקום וסדר (תחנה 5), חלון הקשר (תחנה 6), Decoding כיצירתיות/טמפרטורה (תחנה 13), ולולאת ייצור התשובה טוקן-אחרי-טוקן (תחנה 14, שמתבלבלת היום עם קריאת הקלט מילה-אחרי-מילה בפרק 4).

**הפעולה המרכזית המומלצת:** לתת ל-Attention בית מפורש בתפר שבין פרק 7 (גיאומטריית המשמעות הסטטית) לפרק 8 (ציונים והסתברויות), כלומר **בראש פרק 8**, שכבר מכיל את מעבדת "גל השינוי" שמגלמת את אינטואיציית ה-Attention (טוקנים משפיעים זה על זה, שינוי מילה אחת שולח גל במורד השרשרת). זהו שינוי בינוני בפרק אחד, ללא הוספת פרק, והוא סוגר את הפער המרכזי במקום הנכון בסדר המפה.

**שינוי קריטי נוסף:** להסיר מפרק 1 את מסגור "15 השלבים" הישן (המבוא כבר נטש אותו לטובת "תחנות מרכזיות, ללא מספר קבוע"). פרק 1 צריך להפוך לריצת צ'אט שקופה אחת ומודרכת, לא לחזרה על כל המבוא ולא ללוח של 15 שלבים ממוספרים.

---

## B. ביקורת 16 הפרקים הקיימים (Audit)

| פרק | שם קיים | מה הפרק מלמד היום בפועל | תחנות מבוא קשורות | מיושר למבוא? | בעיה מרכזית | שינוי נדרש |
|---|---|---|---|---|---|---|
| 1 | הדרך אל התשובה | צ'אט שקוף: מאחורי תשובה רץ מסלול שלם; Chat בוחר תשובה, Agent בוחר צעד; ביטחון לפי פער. מעבדה + 4 תת-מעבדות עומק | 1 הבקשה נכנסת, 2 טוקנים, 11-13 ציון/הסתברות/בחירה, 14 לולאה; Agent vs Chat | כן, חלקית | עדיין משתמש במסגור "15 שלבים" שהמבוא נטש; מסלול ברירת המחדל עמוס | שינוי בינוני |
| 2 | ההחלטה הראשונה | ניתוב כוונה: אותו נושא, ניסוח שונה = מסלול שונה (שאלה/בדיקה/פעולה/עצירה); כוונה+סיכון+מידע חסר | 1 הבקשה נכנסת; שכבת Agent (ניתוב) | כן | מקדים את שכבת ה-Agent לפני שהמנוע הפנימי נלמד; תפקידו כ"החלטה ראשונה" צריך מסגור | שינוי קטן |
| 3 | הלב ההסתברותי | AI מעריך ולא יודע; ביטחון = פער בין ראשונה לשנייה; התפלגות מפוזרת = לשאול | 11 Logits, 12 Softmax, 13 Decoding (חלקי) | כן | מצוין; Decoding כ"צפוי מול יצירתי" כמעט לא עולה | שינוי קטן |
| 4 | מילה אחר מילה | קריאת הקלט מצטברת; כל מילה מזיזה הסתברות; שלילה הופכת כיוון; זמני עד Send | 2 טוקנים, 5 מיקום/סדר (חלקי), 11 Logits, 14 לולאה (קריאה) | כן | מבלבל קריאת-קלט מילה-אחרי-מילה עם לולאת ייצור-פלט טוקן-אחרי-טוקן (תחנה 14) | שינוי קטן |
| 5 | טקסט מתפרק (Tokenization) | פירוק לטוקנים כשלב ראשון; טוקן אינו מילה; פיסוק/מורפולוגיה משנים פירוק | 1 הבקשה נכנסת, 2 פירוק לטוקנים, 3 Token IDs (גשר), 5 סדר, 6 חלון הקשר (גשר) | כן, חזק | פרק עוגן מצוין; "מיקום וסדר" ו"חלון הקשר" רק נרמזים | ללא שינוי / שינוי קטן |
| 6 | ממילים למספרים | טוקן -> Token ID (כתובת) -> וקטור משמעות; מילים שונות, אותו כיוון | 3 Token IDs, 4 Embedding | כן, חזק | חזק; אפשר לחבר מפורשות לתחנת Embedding | ללא שינוי |
| 7 | מרחב המשמעות | וקטור כנקודה וחץ; כיוון לפני מרחק; Cosine; אנלוגיה מלך-מלכה | 4 Embedding, (5 מיקום, 7 Attention כפרה-רקוויזיט) | כן, חזק | מצוין כשלעצמו, אך משאיר את אזור C (Attention/הקשר) ללא בית | ללא שינוי / שינוי קטן |
| 8 | דמיון, ציונים והסתברויות | שרשרת: דמיון -> ציון גולמי -> Softmax -> החלטה; גל שינוי במורד השרשרת | 7 Attention (נרמז), 8 ערבוב (נרמז), 11 Logits, 12 Softmax, 13 Decoding | כן, אך חלקי | כאן נמצא הפער: Attention/ערבוב/שכבות לא נלמדים מפורשות למרות שהמעבדה מגלמת אותם | שינוי בינוני (לשקול הרחבה ל-Attention) |
| 9 | מתי לענות ומתי לעצור | ביטחון כשער החלטה; פער מול סף; סיכון מעלה רף; עצירה אינה כישלון | 12 Softmax (קלט), 13 Decoding, החלטה; Agent risk | כן, חזק | חזק; חופף בכוונה עם פרק 3 ו-13 (גשר Chat->Agent) | ללא שינוי |
| 10 | מ-Prompt למשימה | הבנת משימה כשלב ראשון של Agent; מילה אחת הופכת שאלה למשימה; זיהוי אינו אישור | שכבת Agent: פירוש משימה, מידע חסר, החלטה | כן, חזק | חזק | ללא שינוי |
| 11 | בחירת כלי | בחירת כלי רב-גורמית: התאמה, קלט, סיכון, הרשאה; כלי הוא החלטה לא כפתור | שכבת Agent: בחירת כלי, מידע חסר, סיכון, הרשאה | כן, חזק | חזק | ללא שינוי |
| 12 | הפעלת כלי ותוצאה | Agent בלולאה: מחליט, מפעיל, קורא Observation, מחליט שוב; תוצאה מכריעה החלטה | שכבת Agent: Tool Call, Observation, לולאה | כן, חזק | חזק; "לולאה" כאן מחזקת את תחנה 14 ברמת המערכת | ללא שינוי |
| 13 | עצירה ואחריות | שלושה שערי בקרה (סיכון, הרשאה, אישור); קרבה לפעולה מעלה בקרה; עצירה אינה כישלון | שכבת Agent: סיכון, הרשאה, אישור, עצירה | כן, חזק | חזק | ללא שינוי |
| 14 | המעבדה המאוחדת | אותו קלט -> מנוע תשובה (Chat) או מנוע פעולה (Agent) על מסך אחד; מקור אמת אחד מפרקים 6-12 | כל 14 התחנות + לולאת Agent | כן, חזק | קפסטון חזק; כאן צריך לוודא שלולאת ייצור-הפלט (תחנה 14) נראית מפורשות | שינוי קטן |
| 15 | האם AI לומד מטעויות | משקלים קפואים בזמן הסקה; תיקון חי רק בהקשר; שלוש שכבות למידה | מחוץ ל-14 התחנות: אימון מול הסקה (גשר מ-13) | כן | חשוב ונכון; פחות קשור למפת התחנות וזה תקין | ללא שינוי |
| 16 | לעבוד נכון עם AI | כתיבה טובה = הגדרה לא קסם; 5 ממדי איכות; בחירה בין Chat ל-Agent | סינתזה יישומית של כל התחנות והשכבה | כן, חזק | פרק סיום חזק | ללא שינוי |

**מסקנת הביקורת:** 11 מתוך 16 פרקים = ללא שינוי או שינוי קטן. אף פרק אינו מנותק מהמבוא. נקודות הכובד היחידות: פרק 1 (הסרת "15 שלבים"), ופרק 8 (בית ל-Attention/אזור C).

---

## C. ארכיטקטורת הקורס הסופית המומלצת

הוערכו ארבע האפשרויות:

| אפשרות | תיאור | אמת | בהירות | קוהרנטיות עם המבוא | הנאה ללא-מתכנתים | עלות מימוש | סיכון לשבירה | תאימות מבחן/mastery | תמיכה ב-progressive disclosure | פסיקה |
|---|---|---|---|---|---|---|---|---|---|---|
| A | 16 פרקים, שינוי שם/מסגור בלבד | בינונית (משאיר פער Attention) | טובה | טובה | טובה | נמוכה מאוד | נמוך מאוד | מלאה | קיימת | לא מספיק |
| B | 16 פרקים, עיצוב מחדש של מטרות ופתיחים סביב המפה | גבוהה | גבוהה | גבוהה | גבוהה | בינונית | נמוך | מלאה | קיימת | קרוב |
| C | לשנות את מספר הפרקים (למשל להוסיף פרק Attention) | גבוהה מאוד | גבוהה | גבוהה | בינונית (17 שובר "16 פרקים, 5 מערכות") | גבוהה | גבוה (routing/quiz/mastery/intro) | דורשת שינוי | קיימת | נדחה |
| D | היבריד: לשמר 16, פתיחים לפי המפה, ולסגור את פער Attention בתוך פרק קיים | גבוהה | גבוהה | גבוהה מאוד | גבוהה | בינונית-נמוכה | נמוך | מלאה | קיימת ומורחבת | **נבחר** |

**הבחירה: Option D.** Option C נדחה כי הוספת פרק שוברת את ההבטחה המפורשת במבוא ("16 פרקים, 5 מערכות"), את ה-routing, את מיפוי ה-quizData (1-16) ואת ה-mastery, בעלות סיכון גבוה ותועלת נמוכה לעומת D. Option A לא סוגר את פער ה-Attention. D משיג את האמת והקוהרנטיות של B+C בסיכון של A.

**עקרונות המבנה הסופי:**
1. 16 פרקים, 5 מערכות, מספור ו-routing קיימים נשמרים.
2. נרטיב ספירלי מוצהר: מבט-על (1-4) -> צלילה לפי סדר המפה (5-9) -> שכבת Agent (10-13) -> סינתזה (14-16). מותר ונכון שפרקים 1-4 ירמזו לתחנות לפני שהן נלמדות לעומק.
3. כל פרק נקשר במפורש לתחנה/ות שהוא פותח, באמצעות פתיח-סקרנות וכרטיס "מה זה פותח במפה".
4. אזור C (Attention) מקבל בית מפורש בראש פרק 8, במסלול הבסיסי (כדי לעמוד בכלל הוגנות המבחן).

---

## D. תוכנית 16 הפרקים הסופית המומלצת

נשמרים 16 פרקים. לכל פרק: שאלת פתיחה (סקרנות, לא שיווק), התחנות שהוא פותח, מטרת למידה, החוויה המרכזית, 3 נקודות לזכור, מה המבחן צריך לבדוק, ורמת שינוי.

### מערכה 1 - מבט מבחוץ וצ'אט שקוף (פרקים 1-4)

| פרק | שם מומלץ | שאלת פתיחה | תחנות שהפרק פותח | מטרת למידה | החוויה המרכזית | 3 נקודות לזכור | מה 5 השאלות צריכות לבדוק | שינוי |
|---|---|---|---|---|---|---|---|---|
| 1 | הדרך אל התשובה (הצ'אט השקוף) | מה באמת קורה בין הרגע שאתם שולחים הודעה לבין הרגע שהתשובה מופיעה? | 1 הבקשה נכנסת, 14 לולאה (תצוגה), גשר ל-11-13 | להראות שמאחורי תשובה רץ מסלול שקוף, ושיש שני מצבי חשיבה: Chat בוחר תשובה, Agent בוחר צעד | ריצת צ'אט שקופה אחת מודרכת: בקשה -> תחנות מרכזיות -> תשובה; עומק אופציונלי: ראש קריאה, חוגת ביטחון, מה-אם, פיצול Chat/Agent | תהליך ולא קסם; Chat בוחר תשובה ו-Agent בוחר צעד; ביטחון = פער בין ראשונה לשנייה | תהליך מול שליפה ממאגר; Chat מול Agent; ביטחון לפי פער; בנייה חיה; פער+סיכון = אחריות | בינוני |
| 2 | ההחלטה הראשונה | למה אותה חבילה יכולה לשלוח את המנוע לארבעה מסלולים שונים? | 1 הבקשה נכנסת; שכבת Agent (ניתוב) | להראות שהניסוח (לא הנושא) קובע ניתוב, ושהמסלול נולד מכוונה+סיכון+מידע חסר | RequestRoutingLab: שלושה ניסוחים, ארבעה מסלולים; צ'יפים של כוונה/סיכון/מידע חסר נדלקים | ניסוח מקודד כוונה; מסלול = כוונה+סיכון+מידע חסר; פעולה רגישה -> עצירה לאישור | ניתוב לפי כוונה; שלושת הגורמים; מידע חסר לפני פעולה; מערכת עונה מול Agent; כוונת פעולה וסיכון | קטן |
| 3 | הלב ההסתברותי | אם המודל לא "יודע", איך הוא בכל זאת בוחר תשובה? | 11 Logits, 12 Softmax, 13 Decoding (צפוי מול יצירתי) | להפנים ש-AI מעריך סבירות ולא מחזיק אמת; פער קובע ביטחון | ProbabilityEngineLab: אותה כוונה בניסוחים שונים -> התפלגויות חדות/קרובות/מפוזרות | הערכה ולא ידיעה; ביטחון בטוח אינו אמת; פער קטן/מפוזר = לשאול | הערכה מול ידיעה; ביטחון אינו אמת; פער כמדד; התפלגות מפוזרת; אותה התפלגות בחירה צפויה מול יצירתית (Decoding) | קטן |
| 4 | מילה אחר מילה | למה מילה אחת באמצע משפט יכולה להפוך את כל ההחלטה? | 2 טוקנים, 5 מיקום וסדר, 11 Logits, 14 לולאה (קריאה) | להראות שהקלט נקרא מצטבר, כל מילה מזיזה הסתברות, והסדר משנה משמעות | WordEngineLab: הקלדה חיה, "לא" הופך כיוון, זמני עד Send | בנייה מצטברת; שלילה/סדר הופכים כיוון; זמני עד Send | בנייה הדרגתית; השפעת מילה בודדת; זמני מול סופי; כוח המילה הראשונה; הסדר משנה משמעות (תחנה 5) | קטן |

### מערכה 2 - מטקסט לטוקנים ולייצוגים (פרקים 5-7)

| פרק | שם מומלץ | שאלת פתיחה | תחנות שהפרק פותח | מטרת למידה | החוויה המרכזית | 3 נקודות לזכור | מה 5 השאלות צריכות לבדוק | שינוי |
|---|---|---|---|---|---|---|---|---|
| 5 | טקסט מתפרק (Tokenization) | איך טקסט רגיל הופך ליחידות שהמנוע יודע לעבד? | 2 פירוק לטוקנים, 1 הבקשה נכנסת, גשר ל-3, 5, 6 | להבין שהפירוק קודם להבנה, וטוקן אינו בהכרח מילה | TokenizationLab: פירוק חי, תפקידי טוקן, מקרי קצה בעברית | פירוק לפני הבנה; טוקן אינו מילה; פיסוק/צורה משנים פירוק | פירוק כשלב ראשון; טוקן כיחידת עבודה; פיסוק כטוקן; טוקן מול מילה; השפעת הפירוק על ההמשך | ללא/קטן |
| 6 | ממילים למספרים | איך מספר מתחיל לקבל משמעות? | 3 Token IDs, 4 Embedding | להבין ש-ID הוא כתובת ולא משמעות, ושוקטור המשמעות נבנה מהרצף | WordToNumberLab: טקסט -> IDs -> וקטור; מילים שונות, אותו כיוון | המנוע רואה מספרים; ID = כתובת לא משמעות; מילים שונות יכולות להצביע לאותו כיוון | המנוע רואה מספרים; Token ID ככתובת; כיוון משמעות משותף; מהו וקטור משמעות; הווקטור משפיע גם על החלטות | ללא |
| 7 | מרחב המשמעות | למה הכיוון של מילה חשוב יותר מהמרחק שלה? | 4 Embedding, גשר ל-7 Attention | אינטואיציה גיאומטרית: כיוון לפני מרחק; יחסים כחשבון חצים | SemanticSpaceLab: גרירה במרחב; אנלוגיית מלך-מלכה | וקטור = נקודה וחץ; קרבה לפי זווית; יחסים = חשבון חצים | וקטור כנקודה וחץ; כיוון לפני מרחק; קרבה תלויה בזווית; דמיון קוסינוס; יחסים כחשבון חצים | ללא/קטן |

### מערכה 3 - הקשר, ציונים והסתברויות (פרקים 8-9)

| פרק | שם מומלץ | שאלת פתיחה | תחנות שהפרק פותח | מטרת למידה | החוויה המרכזית | 3 נקודות לזכור | מה 5 השאלות צריכות לבדוק | שינוי |
|---|---|---|---|---|---|---|---|---|
| 8 | הקשר, דמיון וציונים | איך המודל יודע למי המילה "הוא" מתייחסת, ומאיפה בכלל מגיעים האחוזים? | 7 Attention, 8 ערבוב מידע, 9 שכבות (קל), 10 ייצוג עדכני (קל), 11 Logits, 12 Softmax, 13 Decoding | להראות שההקשר מעצב מחדש את משמעות כל מילה (Attention), ורק אז נמדד דמיון, ציון ו-Softmax | PipelineCascadeLab בתוספת פתיח Attention: "כל מילה בודקת אילו מילים אחרות חשובות לה" + גל שינוי במורד השרשרת | הקשר משנה משמעות (Attention); דמיון אינו הסתברות; Softmax הופך ציונים לאחוזים | מהו Attention (הקשר משנה משמעות); שרשרת ולא קפיצה; תפקיד Softmax; ציון גולמי אינו אחוז; דמיון מול הסתברות | בינוני |
| 9 | ביטחון ושער ההחלטה | מתי נכון לענות, ומתי נכון לעצור ולשאול? | החלטה, 13 Decoding (קלט); Agent risk | ביטחון כשער: פער מול סף, וסיכון מעלה רף | ConfidenceGateLab: סליידר סף, מצב Chat מול Agent עם סיכון | פער מול סף קובע פעולה; סיכון מעלה רף; עצירה אינה כישלון | פער ביטחון; שער החלטה; סף וסיכון; סיכון ב-Agent; ביטחון נמוך ופעולה | ללא |

### מערכה 4 - מ-Chat ל-Agent (פרקים 10-13)

| פרק | שם מומלץ | שאלת פתיחה | תחנות שהפרק פותח | מטרת למידה | החוויה המרכזית | 3 נקודות לזכור | מה 5 השאלות צריכות לבדוק | שינוי |
|---|---|---|---|---|---|---|---|---|
| 10 | מ-Prompt למשימה | מתי שאלה הופכת למשימה, ומה Agent עושה ברגע הזה? | שכבת Agent: פירוש משימה, מידע חסר | Agent מתחיל מהבנת משימה; זיהוי אינו אישור | TaskUnderstandingLab: פירוק בקשה למטרה/חסר/סיכון/החלטה | מילה אחת הופכת שאלה למשימה; Agent מתחיל בהבנה; זיהוי אינו אישור | שאלה מול משימה; מסלול ה-Agent; זיהוי אינו אישור; מידע חסר; שאלה/הבהרה/אישור | ללא |
| 11 | בחירת כלי | מתי Agent צריך כלי, ומתי דווקא לא? | שכבת Agent: בחירת כלי, סיכון, הרשאה | בחירת כלי כהחלטה רב-גורמית (התאמה/קלט/סיכון/הרשאה) | ToolSelectionLab: דירוג כלים מול ארבעה שערים | ארבעה שערים; מתאים אך חסום; אי-שימוש לעיתים נכון | ארבעת השערים; ניחוש מול בדיקה; אי-שימוש כצעד נכון; מתאים אך חסום; כלי הוא החלטה | ללא |
| 12 | הפעלת כלי ותוצאה | למה Agent פועל בלולאה ולא בקו ישר? | שכבת Agent: Tool Call, Observation, לולאה | Tool Call מביא Observation; התוצאה מכריעה את ההחלטה הבאה | ObservationLoopLab: אותה קריאה, תוצאות שונות, החלטות שונות | Tool Call מביא תצפית; לולאה לא קו ישר; תוצאה חלקית/סותרת = זהירות | Tool Call; לולאת ה-Agent; תוצאה מכריעה; תוצאה חלקית; שער סיכון אחרי תוצאה | ללא |
| 13 | עצירה, אישור ואחריות | מתי Agent צריך לעצור ולא לפעול, גם כשהוא יכול? | שכבת Agent: סיכון, הרשאה, אישור, עצירה | שלושה שערי בקרה; קרבה לפעולה מעלה אחריות; יכולת אינה הרשאה | ControlLayerLab: אותה יכולת, החלטה שונה לפי סיכון/הרשאה/אישור | שלושת שערי הבקרה; קרבה לפעולה מעלה בקרה; יכולת אינה הרשאה | עצירה כאחריות; שלושת השערים; קרבה לפעולה; החלטה לפי סיכון; ביטחון אינו אישור | ללא |

### מערכה 5 - סינתזה ומיומנות (פרקים 14-16)

| פרק | שם מומלץ | שאלת פתיחה | תחנות שהפרק פותח | מטרת למידה | החוויה המרכזית | 3 נקודות לזכור | מה 5 השאלות צריכות לבדוק | שינוי |
|---|---|---|---|---|---|---|---|---|
| 14 | המעבדה המאוחדת | אם נראה את הכל על מסך אחד, איך זה מתחבר לתמונה אחת? | כל 14 התחנות + לולאת Agent, דגש על תחנה 14 (ייצור טוקן-אחרי-טוקן) | לאחד את כל התחנות; אותו קלט -> Chat או Agent; מקור אמת אחד | BehindScenesLab: צינור Chat מלא וצינור Agent מלא על מסך אחד, Replay | אותו קלט, שני מנועים; כל מספר מהפרקים הקודמים; ארבע התנהגויות אפשריות | שתי צורות חשיבה; מקור אמת אחד; ארבע התנהגויות; שתי רמות הבנה; החוטים הנשמרים (דמיון אינו הסתברות, יכולת אינה הרשאה) | קטן |
| 15 | האם AI לומד מטעויות | כשאני מתקן את המודל, האם הוא באמת לומד ממני? | מחוץ ל-14 התחנות: אימון מול הסקה | משקלים קפואים בהסקה; תיקון חי רק בהקשר; שלוש שכבות למידה | LearnsFromMistakesLab: משקלים קפואים, תיקון שנעלם, שלוש שכבות | משקלים קפואים; הקשר אינו זיכרון; שיפור אמיתי = גרסה חדשה | משקלים קפואים; התיקון שנעלם; התאמה זמנית; שלוש שכבות למידה; לא לומד חי אך כן למד | ללא |
| 16 | לעבוד נכון עם AI | אם המודל לא קורא מחשבות, איך מנסחים בקשה שתעבוד? | סינתזה יישומית של כל התחנות והשכבה | כתיבה טובה = הגדרה; בחירה נכונה בין Chat ל-Agent | PromptCoachLab: מאמן בקשות, מד איכות 5 ממדים, בורר Chat/Agent | הגדרה ולא קסם; הגדרה מפחיתה ניחוש; לא כל בקשה צריכה Agent | כתיבה כהגדרה; הגדרה מפחיתה ניחוש; כלי אימון; שאלת הבהרה תקינה; בחירה בין Chat ל-Agent | ללא |

---

## E. תוכנית יעדי מבחן (5 שאלות) לכל פרק

הכלל הקשיח: כל יעד שאלה חייב להיבדק רק על תוכן שנמצא במסלול ברירת המחדל או בכרטיס סיכום גלוי לפני המבחן. שום שאלת חובה אינה נשענת על עומק אופציונלי בלבד.

| פרק | יעד 1 (מושג) | יעד 2 (יישום/תרחיש) | יעד 3 (הבחנה בין דומים) | יעד 4 (זיהוי טעות נפוצה) | יעד 5 (חיבור למפת המנוע) | misconception מרכזי |
|---|---|---|---|---|---|---|
| 1 | מאחורי תשובה רץ מסלול שלם | תרחיש: פער גדול מול קטן בבקשה רגישה -> לסמוך/לעצור | תהליך מול שליפה ממאגר | "התשובה נשלפת ממאגר מוכן" | המסלול מהבקשה ועד הלולאה | "AI שולף תשובה מוכנה" |
| 2 | מסלול = כוונה+סיכון+מידע חסר | "שלח ללקוח..." מול "איפה החבילה?" -> מסלול שונה | מערכת שעונה מול Agent שמנתב | "אורך/צורת המשפט קובעים מסלול" | ניתוב כשלב ראשון של שכבת Agent | "הנושא קובע את המסלול" |
| 3 | הערכת סבירות ולא ידיעה | התפלגות מפוזרת -> לשאול ולא לנחש | ביטחון בניסוח מול נכונות | "AI תמיד בוחר את המספר הגבוה = העיקר" | פער/התפלגות מזינים את שער ההחלטה (Decoding) | "תשובה בטוחה = תשובה נכונה" |
| 4 | משפט נבנה מילה אחר מילה | הקלדת "החבילה" ואז "לא" -> קפיצת עמודות | זמני מול סופי (עד Send) | "המנוע קורא רק את המילה האחרונה" | כל מילה מזיזה Logits; הסדר משנה משמעות | "המנוע מעבד את המשפט כגוש אחד" |
| 5 | פירוק לטוקנים לפני הבנה | "החבילה לא הגיעה" כרצף יחידות עבודה | טוקן מול מילה | "מילה אחת = טוקן אחד תמיד" | הפירוק מזין את כל התחנות הבאות | "פירוק הוא רק פורמליות טכנית" |
| 6 | Token ID ככתובת ולא משמעות | שני משפטים שונים, וקטור כמעט זהה | Token ID מול וקטור משמעות | "Token ID הוא המשמעות" | וקטור המשמעות הוא הייצוג שכל התחנות מחשבות עליו | "המודל צריך בדיוק אותן מילים" |
| 7 | קרבה לפי זווית ולא מרחק | גרירת מילה לאורך אותו כיוון -> קרבה לא משתנה | מרחק מול זווית (Cosine) | "מילה רחוקה מהראשית = רחוקה במשמעות" | משמעות כ-Embedding (כיוון במרחב) | "מרחק = חוסר דמיון" |
| 8 | Attention: ההקשר משנה משמעות מילה | שינוי מילה אחת -> גל במורד השרשרת -> ההחלטה מתהפכת | דמיון מול הסתברות; ציון גולמי מול אחוז | "דמיון הוא ההסתברות" | מהשרשרת Attention->דמיון->ציון->Softmax נגזרת ההחלטה | "האחוזים מופיעים מהאוויר / דמיון = הסתברות" |
| 9 | ביטחון כשער (פער מול סף) | פעולה רגישה עם פער קטן -> לעצור | פער מול ערך מוחלט של המוביל | "ביטחון גבוה תמיד מצדיק פעולה" | שער ההחלטה אחרי Softmax, גשר ל-Agent | "פער קטן = כל האפשרויות טובות" |
| 10 | הבנת משימה כשלב ראשון | "בדוק..." חסר ברקוד -> לבקש מידע | שאלה מול משימה | "זיהוי משימה = אישור לפעול" | פירוש המשימה פותח את לולאת ה-Agent | "Agent מתחיל מהפעלת כלי" |
| 11 | בחירת כלי = ארבעה שערים | כלי מדורג ראשון אך חסום בקלט/הרשאה | שימוש מיותר מול אי-שימוש נכון | "כלי זמין = חייב להפעיל" | בחירת כלי בתוך מסלול ה-Agent | "אם יש כלי, תמיד עדיף להפעיל" |
| 12 | Tool Call מביא Observation | אותה קריאה, תוצאה חלקית מול ברורה -> החלטה שונה | תוצאה ברורה/חלקית/סותרת | "Tool Call הוא סוף הסיפור" | הלולאה: מחליט->מפעיל->קורא->מחליט (תחנה 14 ברמת מערכת) | "התוצאה ידועה מראש ל-Agent" |
| 13 | שלושה שערי בקרה (סיכון/הרשאה/אישור) | אותה יכולת -> תשובה/טיוטה/עצירה לפי סיכון | יכולת מול הרשאה | "ביטחון גבוה מבטל אישור" | שכבת הבקרה סוגרת את מסלול ה-Agent | "אם הוא יכול, מותר לו" |
| 14 | אותו קלט, שני מנועים | אותה תיבה -> תשובה/הבהרה/כלי/עצירה | שתי רמות הבנה (קלט-פלט מול מסלול שלם) | "המעבדה ממציאה מספרים חדשים" | כל התחנות + הלולאה במקום אחד, מקור אמת אחד | "כל מסך הוא מנוע נפרד" |
| 15 | משקלים קפואים בהסקה | תיקון נעלם בשיחה חדשה | התאמה זמנית מול למידה אמיתית | "המודל לומד ממני חי" | אימון (קפוא) מול הסקה; גשר משכבת הבקרה | "המודל זוכר אותי / או: קפוא לגמרי" |
| 16 | כתיבה טובה = הגדרה | בקשה חלשה -> שיפור לפי 5 ממדים | Chat (תשובה) מול Agent (פעולה) | "Prompt ארוך = תוצאה טובה" | חיבור הבנת המנוע לניסוח טוב | "מילות קסם משכנעות את המודל" |

---

## F. כיסוי 14 תחנות ה-LLM

| תחנה במבוא | פרק/ים שמלמדים אותה | כיסוי מספיק? | מה חסר | המלצה |
|---|---|---|---|---|
| 1 הבקשה נכנסת | 1, 2, 5 | כן | אזכור מפורש שהבקשה נכנסת עם הוראות מערכת + היסטוריית שיחה | להוסיף משפט קצר בפרק 1/5 (תחנה 6) |
| 2 פירוק לטוקנים | 5 (ייעודי), 1, 4 | כן, חזק | - | ללא שינוי |
| 3 Token IDs | 6 (ייעודי) | כן, חזק | - | ללא שינוי |
| 4 Embedding | 6, 7 (ייעודי) | כן, חזק | - | ללא שינוי |
| 5 מיקום וסדר | 4 (נרמז), 5 (נרמז) | חלקי | אין דוגמה מפורשת "אדם נשך כלב" מול "כלב נשך אדם" | להוסיף דוגמת סדר קצרה למסלול הבסיסי של פרק 4 או 5 |
| 6 חלון הקשר | 5 (גשר), 8 (Agent context) | חלקי | המושג "חלון הקשר/היסטוריה/הוראות מערכת" אינו נלמד מפורשות | להוסיף כרטיס מושג קצר בפרק 1 או 8 (בסיסי) |
| 7 Attention | 8 (נרמז במעבדה), 3 | **לא מספיק (פער מרכזי)** | אין הוראה מפורשת ש"כל טוקן בודק אילו טוקנים חשובים לו" / קורפרנס "הוא" | **לפתוח את ראש פרק 8 ב-Attention במסלול הבסיסי** |
| 8 ערבוב מידע | 8 (גל שינוי, נרמז) | חלקי | "המידע מתערבב ומעדכן את הייצוג" לא נאמר מפורשות | לכלול בפתיח ה-Attention של פרק 8 |
| 9 שכבות Transformer | - | **לא מספיק** | "אותו עיבוד חוזר בשכבות" אינו מופיע | כרטיס מושג קל בפרק 8 (אפשר עומק) + אזכור בפרק 14; להישען על TRUTH_NOTE |
| 10 ייצוג פנימי עדכני | 6, 8 (נרמז) | חלקי | "מצב פנימי שמסכם הקשר" לא ממוסגר ככזה | לקשור בפרק 8 את הווקטור-בהקשר לתחנה הזו |
| 11 Logits | 8 (ציון גולמי, ייעודי), 3 | כן, חזק | - | ללא שינוי |
| 12 Softmax והסתברויות | 8 (ייעודי), 3 | כן, חזק | - | ללא שינוי |
| 13 Decoding ובחירת הטוקן הבא | 8, 9, 3 (חלקי) | חלקי | "אותה התפלגות -> בחירה צפויה מול יצירתית" (טמפרטורה) כמעט לא עולה במסלול הבסיסי | להוסיף ביט Decoding קצר לפרק 3 או 8 (בסיסי) |
| 14 לולאה עד תשובה | 1 (תצוגה), 12 (Agent), 14 | חלקי | מתבלבל עם קריאת-קלט מילה-אחרי-מילה (פרק 4); ייצור-הפלט טוקן-אחרי-טוקן לא מודגם ברור | להבהיר בפרק 1/14 שהתשובה נבנית טוקן-אחרי-טוקן בלולאה עד סימן עצירה |

**דגל פערים (לפי דרישת המבוא):** Attention, שכבות Transformer, ולולאת ייצור-הפלט מובטחים במבוא אך אינם נלמדים מספיק כיום. Decoding כיצירתיות חסר במסלול הבסיסי. מיקום/סדר וחלון הקשר נרמזים בלבד.

---

## G. כיסוי מושגי Agent

| Agent concept | פרק/ים | כיסוי מספיק? | מה חסר | המלצה |
|---|---|---|---|---|
| פירוש משימה (task interpretation) | 10 (ייעודי), 2 | כן, חזק | - | ללא שינוי |
| בדיקת מידע חסר | 10, 11, 2 | כן, חזק | - | ללא שינוי |
| בחירת כלי | 11 (ייעודי) | כן, חזק | - | ללא שינוי |
| הפעלת כלי (Tool Call) | 12 (ייעודי) | כן, חזק | - | ללא שינוי |
| תצפית/תוצאה (Observation) | 12 (ייעודי) | כן, חזק | - | ללא שינוי |
| הערכת סיכון | 11, 12, 13, 2 | כן, חזק | - | ללא שינוי |
| הרשאה/אישור | 11, 13 (ייעודי) | כן, חזק | - | ללא שינוי |
| החלטת עצירה/המשך | 9, 12, 13, 10 | כן, חזק | - | ללא שינוי |
| Agent מול Chat | 1 (ייעודי), 14, 16 | כן, חזק | - | ללא שינוי |
| Agent כשכבת מערכת סביב המודל | 1, 2, 14 | כן | אפשר חידוד מפורש ש"זו אינה שכבה פנימית של ה-Transformer" (כמו במבוא) | אזכור משפט אחד בפרק 10 או 14 |

**מסקנה:** כיסוי ה-Agent מצוין ושלם. זהו החלק החזק ביותר של הלומדה ואינו דורש שינוי מבני.

---

## H. המלצת 5 מערכות / מערכות-על

הקיבוץ הקיים נכון ונשמר. שינוי יחיד מומלץ: לרענן את שם מערכה 3 כך שיחשוף את Attention/ההקשר, בהתאם לפתיחת פרק 8.

| מערכה | שם מומלץ | פרקים | למה הקיבוץ נכון | מה הלומד מרגיש בסוף |
|---|---|---|---|---|
| 1 | מבט מבחוץ וצ'אט שקוף | 1-4 | פותח את האשליה "בקשה ותשובה" ונותן מבט-על על המנוע ועל Chat מול Agent לפני הצלילה | "יש מסלול שלם מאחורי כל תשובה, ואני כבר רואה אותו" |
| 2 | מטקסט לטוקנים ולייצוגים | 5-7 | המסע מטקסט -> טוקנים -> IDs -> וקטורים -> מרחב משמעות, לפי סדר המפה | "הבנתי איך טקסט הופך למשהו שאפשר לחשב עליו" |
| 3 | הקשר, דמיון, ציונים והסתברויות | 8-9 | ההקשר (Attention) מעצב משמעות, וממנו נגזרים ציונים, הסתברויות ושער ביטחון | "אני מבין מאיפה באים האחוזים, ומתי לסמוך עליהם" |
| 4 | מ-Chat ל-Agent | 10-13 | שכבת המערכת סביב המודל: משימה, כלי, לולאה, בקרה | "אני מבין מה משתנה כשהמודל גם פועל, לא רק כותב" |
| 5 | סינתזה ומיומנות | 14-16 | איחוד כל התחנות, הבנת גבולות הלמידה, ותרגול עבודה חכמה | "אני יודע לעבוד עם AI מתוך הבנה, לא מתוך קסם" |

הערה: שם מערכה 3 ב-COURSE_SYSTEMS היום הוא "הקשר, ציונים והסתברויות" ו-stationChips כולל Attention. הוספת המילה "דמיון" וחיזוק ה-Attention בפרק 8 מיישרים בין השם להבטחה.

---

## I. תוכנית מיוחדת לפרק 1

פרק 1 הוא הגשר מהמבוא והבטחתו. הוא חייב להמשיך את ההבטחה בלי לחזור על כל המבוא ובלי המסגור הישן של "15 שלבים".

- **שם מומלץ:** "הדרך אל התשובה - הצ'אט השקוף" (נשמר; תואם courseData ו-quizData).
- **שאלת פתיחה:** "מה באמת קורה בין הרגע שאתם שולחים הודעה לבין הרגע שהתשובה מופיעה?"
- **מה מופיע במסלול ברירת המחדל:**
  - ריצת צ'אט שקופה אחת ומודרכת על בקשה אמיתית: בקשה נכנסת -> כמה תחנות מרכזיות (טוקנים, דירוג/הסתברות, ביטחון) -> תשובה שנבנית טוקן-אחרי-טוקן.
  - הבחנה מרכזית אחת ויחידה: Chat בוחר תשובה, Agent בוחר את הצעד הנכון הבא.
  - כרטיס סיכום גלוי לפני המבחן עם 3 הנקודות (תהליך ולא קסם; Chat מול Agent; ביטחון לפי פער).
- **מה מופיע בעומק אופציונלי:** ארבע תת-המעבדות הקיימות (ראש קריאה, חוגת ביטחון, מה-אם/Counterfactual, פיצול Chat/Agent). נשארות, אך מתחת לשער עומק.
- **מה צריך להסיר או למסגר מחדש:**
  - **להסיר את מסגור "15 השלבים"** (מספר קבוע) ולהחליפו ב"תחנות מרכזיות", בהתאם למבוא ול-TRUTH_NOTE.
  - לא לחזור על כל 14 התחנות של המבוא; פרק 1 הוא ריצה אחת מודרכת, לא מפה חוזרת.
  - לפשט את מסלול ברירת המחדל כך שלא יוצף (העומק קיים, אבל לא כברירת מחדל).
- **מה 5 שאלות המבחן צריכות לבדוק:** תהליך מול שליפה ממאגר; Chat מול Agent; ביטחון לפי פער; בנייה חיה של ההערכה; פער+סיכון = אחריות. (חמש השאלות הקיימות עומדות ביעדים; יש לוודא שאף שאלה אינה תלויה במספר "15".)
- **רמת שינוי:** בינוני (בעיקר הסרת מסגור 15-שלבים ופישוט מסלול הבסיס; תת-המעבדות כבר קיימות).
- **הערות מימוש:** לוודא שכרטיס הסיכום וכל יעדי המבחן נשענים על מסלול ברירת המחדל בלבד; להשאיר את ה-StickyContextBar; לשמור על תאימות ל-quizData הקיים (אין צורך לשנותו).

---

## J. פערים וסיכונים

**פערי תוכן (לפי עדיפות):**
1. **Attention/אזור C (מרכזי):** התחנה הבולטת ביותר במבוא ללא בית פרקי ברור. פתרון: ראש פרק 8 במסלול הבסיסי.
2. **שכבות Transformer + ייצוג פנימי עדכני:** מושגים מתקדמים; כיסוי קל מספיק (כרטיס מושג + הישענות על TRUTH_NOTE). סיכון נמוך אם נשמרים כקלים.
3. **לולאת ייצור-הפלט (תחנה 14):** סיכון בלבול עם פרק 4 (קריאת קלט). להבהיר בפרק 1/14.
4. **Decoding כיצירתיות/טמפרטורה:** חסר במסלול הבסיסי; ביט קצר בפרק 3 או 8.
5. **מיקום/סדר וחלון הקשר:** נרמזים; דוגמה/כרטיס קצרים מספיקים.

**סיכוני מימוש:**
- **הוגנות מבחן:** אם Attention ייבדק במבחן, הוא חייב להיות במסלול הבסיסי או בכרטיס הסיכום של פרק 8, לא בעומק בלבד. זהו האילוץ המחייב ביותר.
- **עומס קוגניטיבי בפרק 8:** הפרק כבר כבד (מעבדת מפל). הוספת Attention מחייבת progressive disclosure: אינטואיציית Attention בסיסית למעלה, מכניקת השכבות בעומק.
- **שבירת mastery/quizData:** כל שינוי מטרה שמשנה את כותרות ה-concept ב-quizData עלול לפגוע במיפוי החזרה הממוקד (CONCEPT_TO_CHAPTER) ובלוח ההתקדמות. יש לשמר את שמות ה-concept הקיימים או לעדכן את שני המקומות יחד.
- **חפיפה מכוונת:** פרקים 1, 3, 9, 13 חולקים את רעיון "ביטחון/עצירה". זו ספירליות מכוונת, לא כפילות; אין למחוק אותה.
- **עקביות שפה:** אין מקף ארוך (U+2014) בשום טקסט חדש.

---

## K. סדר מימוש מומלץ

מסודר לפי יחס ערך-לסיכון, מהבטוח והמשפיע ביותר ועד המתקדם:

1. **פרק 1 - הסרת מסגור "15 שלבים" ופישוט מסלול הבסיס.** השפעה גבוהה על המשכיות מהמבוא, סיכון נמוך. לוודא שה-quiz אינו תלוי במספר.
2. **פרק 8 - פתיחת Attention במסלול הבסיסי (סגירת הפער המרכזי).** להוסיף ביט "ההקשר משנה משמעות / כל טוקן בודק אילו טוקנים חשובים לו" לפני הדמיון, ולקשר לגל השינוי הקיים. לעדכן את כרטיס הסיכום ויעד מבחן אחד.
3. **פתיחי פרקים + כרטיסי "מה זה פותח במפה"** לכל 16 הפרקים (שינוי קטן, אחיד), לחיזוק היישור הגלוי למפה.
4. **פערים קלים:** דוגמת סדר (פרק 4/5), כרטיס חלון הקשר (פרק 1/8), ביט Decoding (פרק 3/8), הבהרת לולאת ייצור-הפלט (פרק 1/14).
5. **שכבות Transformer + ייצוג פנימי עדכני:** כרטיס מושג קל (עומק) בפרק 8 ואזכור בפרק 14, בהישענות על TRUTH_NOTE.
6. **רענון שם מערכה 3** ("הקשר, דמיון, ציונים והסתברויות") ב-COURSE_SYSTEMS, בסנכרון עם פתיחת פרק 8.
7. **בדיקת רגרסיה של mastery/quiz:** לוודא ש-CONCEPT_TO_CHAPTER, קישורי החזרה ולוח ההתקדמות עקביים אחרי כל שינוי כותרות concept.

אחרי כל שלב: בדיקת הוגנות מבחן (כל יעד שאלה נשען על מסלול ברירת מחדל או כרטיס סיכום גלוי), ובדיקת היעדר מקף ארוך.
