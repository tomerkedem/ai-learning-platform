# תוכנית מימוש ממוקדת - פרק 1 ופרק 8 בלבד

מסמך תכנון. לא בוצע שום שינוי בקוד, ב-quizData, ב-courseData, ב-mastery או ב-routing.
אושר Option D: לשמר 16 פרקים, routing, mastery/progress ומבנה quizData. לא להוסיף/להסיר פרקים.

שני התיקונים בעדיפות:
1. פרק 1 - הסרת מסגור "15 שלבים", הפיכתו לריצת צ'אט שקופה אחת מודרכת, "תחנות מרכזיות" במקום מספר קבוע, בלי לחזור על כל המבוא.
2. פרק 8 - בית מפורש ל-Attention במסלול ברירת המחדל; הלומד מבין איך הקשר משנה משמעות (למשל "הוא" -> "הכלב"); גלוי לפני המבדק, לא רק בעומק.

---

## חלק 1 - פרק 1: הסרת "15 שלבים" + ריצת צ'אט שקופה מודרכת

### קבצים לשינוי
- `app/behind-the-scenes-ai/chapter-1/page.tsx` - שורה 362 (subtitle "15 שלבים"), שורה 155 (הערה), חידוד טקסט hero/insight לפי הצורך.
- `app/behind-the-scenes-ai/chapter-1/GlassEnginePanel.tsx` - שורה 364 (`{steps.length} שלבים` בפוטר); שורות 290-304 כבר בונות רשימת `acts` (מערכות) שתשמש למסגור "תחנות מרכזיות".
- `app/behind-the-scenes-ai/chapter-1/engineTrace.ts` - שורה 3 (הערת "15 תהליכים"); אפשר רענון `note` לשלבים. לא משנים את מספר השלבים בפועל, רק את המסגור החיצוני.

### הבעיה הנוכחית
- המבוא נטש מספר קבוע לטובת "תחנות מרכזיות", אך פרק 1 עדיין מכריז "Chat Mode / החלטת פעולה · 15 שלבים" (page.tsx:362) ו"{steps.length} שלבים" בפוטר הלוח (GlassEnginePanel.tsx:364). סתירה ישירה למבוא.
- מסלול ברירת המחדל כבר נקי (deepOpen=false; 4 מעבדות מתחת לשער עומק). הבעיה היא המסגור הויזואלי שמספר שלבים במקום להציג מערכות/תחנות.

### זרימת הפרק המוצעת
1. Hero - ללא שינוי מהותי (השאלה "מה באמת קורה בין השאלה לתשובה" עונה לדרישה).
2. הצ'אט השקוף (ברירת מחדל) - ללא שינוי פונקציונלי. subtitle מנוע התשובה: "בחירת תשובה · תחנות מרכזיות" / "החלטת פעולה · תחנות מרכזיות".
3. פוטר הלוח - במקום "{steps.length} שלבים" להציג את שמות המערכות הקיימות ב-`acts` (קליטה -> ניתוח -> הכרעה -> פלט) או "תחנות מרכזיות" בלי מספר.
4. שכבת העומק (4 מעבדות) - ללא שינוי, נשארת מתחת לשער "פתחו את המנוע המלא".
5. כרטיס לפני המבדק (3 נקודות) - ללא שינוי בתוכן; לוודא שאין רמיזה למספר שלבים.

### מה נשאר ללא שינוי
המנוע החי (`runChatEngine`/`runAgentEngine`), ה-trace, ה-API החי, ספירת הטוקנים, ה-routing, ה-StickyContextBar, ארבע תת-המעבדות, ה-InsightBox, מבנה ה-deepOpen, מספר שלבי ה-trace (15) בפועל, ו-quizData פרק 1.

### יעדי מבחן שנשארים תקפים
כל 5 השאלות תקפות (אף אחת לא בודקת את המספר 15):
1. תהליך מול שליפה ממאגר
2. Chat מול Agent
3. ביטחון לפי פער
4. קריאה חיה (ההערכה נבנית תוך כדי)
5. ביטחון פוגש אחריות

הערה: הסבר שאלה 4 מזכיר "ראש הקריאה" (שכבת עומק), אך הרעיון מעוגן בכרטיס שלפני המבדק (נקודה 1), ולכן עומד בכלל ההוגנות.

### סיכונים
- נמוך: ברובו מחרוזות תצוגה. סיכון עיקרי - להשאיר אזכור "15" באחד משלושת המקומות. לסרוק את כל הפרק.
- אם מציגים שמות מערכות בפוטר במקום מספר, לוודא שאין שבירת layout (GlassEnginePanel כבר מחזיק `acts`).

### צעדי ולידציה
1. grep ל-"15" ו"שלבים" בכל `chapter-1/`; לוודא שלא נשאר מסגור מספרי.
2. בדיקה ויזואלית: subtitle ופוטר מציגים תחנות/מערכות, לא מספר.
3. הרצת הפרק ב-Chat וב-Agent; פתיחה/סגירה של שכבת העומק.
4. הרצת מבדק פרק 1 (5 שאלות).
5. בדיקת היעדר מקף ארוך (U+2014) בכל טקסט שנגעו בו.

---

## חלק 2 - פרק 8: בית מפורש ל-Attention במסלול ברירת המחדל

### קבצים לשינוי
- `app/behind-the-scenes-ai/chapter-8/page.tsx` - `CHAIN` (17-24), `READING_STEPS` (51-58), טקסט hero (197-201), `BEFORE_QUIZ_POINTS` (127-140), InsightBox סיכום (265-273).
- `app/behind-the-scenes-ai/chapter-8/pipelineData.ts` - `ROADMAP_STEPS_7` (425-432): כיום קופץ מ"וקטורים" ישר ל"דמיון" ומדלג על אזור C.
- רכיב הדגמה קטן חדש (מומלץ): `components/ai-internals/ContextMeaningDemo.tsx` או תת-מקטע בתוך הפרק להדגמת קורפרנס "הוא -> הכלב". לא לשנות את `PipelineCascadeLab` הכבד בשלב זה.

### הבעיה הנוכחית
- `ROADMAP_STEPS_7` = טקסט -> טוקנים -> מזהי טוקן -> וקטורים -> דמיון -> ציונים -> הסתברויות. אין תחנת "הקשר/Attention" בין הווקטורים לדמיון. זהו אזור C החסר במפת המבוא.
- Attention מובטח בולט במבוא ("מילה כמו 'הוא' יודעת למי היא מתייחסת") אך אינו נלמד מפורשות. מעבדת "גל השינוי" כבר מגלמת את האינטואיציה אך לא ממסגרת אותה כ-Attention.

### זרימת הפרק המוצעת
הוספת תחנה אחת מפורשת בראש השרשרת, במסלול הבסיסי, לפני "דמיון":
1. Hero + ChainStrip - צומת ראשון חדש "הקשר (Attention)" לפני וקטור/דמיון, עם משפט hero: "לפני שמודדים דמיון, ההקשר מעדכן את משמעות כל מילה - כל מילה בודקת אילו מילים אחרות חשובות לה".
2. ReadingPath - להוסיף את Attention כתחנה מקדימה ("שלב 0") כדי לא לשבור את התאמת המספור 1-6 למעבדה, או למספר מחדש בשני המקומות יחד.
3. ביט Attention ממוקד (חדש, ברירת מחדל) - הדגמה אינטראקטיבית קטנה אחת לפני מעבדת המפל: "הכלב רץ כי הוא רעב"; לחיצה על "הוא" מדגישה את "הכלב" ומראה את הקשר. זו ההמחשה לדרישת המשתמש, לפני המבדק ולא בעומק.
4. PipelineCascadeLab - ללא שינוי פונקציונלי; אפשר משפט מסגור שמחבר את גל השינוי ל-Attention.
5. Roadmap - להוסיף צומת "הקשר/Attention" ל-`ROADMAP_STEPS_7` (בין "וקטורים" ל"דמיון"), active: true.
6. כרטיס לפני המבדק - נקודה רביעית "ההקשר משנה משמעות": "אותה מילה מקבלת משמעות שונה לפי מה שסביבה; כל מילה בודקת אילו מילים אחרות חשובות לה (Attention), ורק אז נמדד דמיון". כך הרעיון גלוי לפני המבדק.

### מה נשאר ללא שינוי
`PipelineCascadeLab`, `CASCADE_LAYERS`, `scoringEngine`, מנוע ה-Agent של הפרק, ה-routing, ה-mastery, מבנה הפרק הכללי, ו-quizData פרק 8 (במעבר הזה).

### יעדי מבחן שנשארים תקפים
כל 5 השאלות נשארות תקפות ואינן תלויות בתוכן Attention החדש:
1. שרשרת ולא קפיצה
2. תפקיד Softmax
3. ציונים גולמיים אינם אחוז
4. גל שינוי בשרשרת (מתחזק עם מסגור Attention)
5. דמיון מול הסתברות

החלטה לגבי שאלת Attention: הכלל הקשיח אוסר לבדוק מושג שאינו במסלול/בסיכום, אך אינו מחייב להוסיף שאלה. כדי לכבד "שמירת מבנה quizData" ולמזער סיכון, בשלב זה לא נוסיף/נשנה שאלה. Attention ייכנס למסלול ולכרטיס שלפני המבדק כתוכן גלוי. אם בעתיד יידרש שהמבדק יבדוק Attention - פעולה נפרדת שתיגע ב-quizData (שאלה אחת) + CONCEPT_TO_CHAPTER + בדיקת רגרסיה של לוח ההתקדמות.

### סיכונים
- עומס קוגניטיבי: פרק 8 כבד. Attention נכנס כביט קצר וממוקד אחד (קורפרנס), לא כמעבדה שנייה מלאה. מכניקת השכבות/Transformer נשארת קלה (כרטיס מושג או עומק).
- שבירת התאמת מספור בין `READING_STEPS` למעבדה: מיטיגציה - להוסיף את Attention כתחנה מקדימה מחוץ למספור 1-6, או למספר מחדש בשני המקומות יחד.
- שינוי `ROADMAP_STEPS_7` מרחיב את הפס הויזואלי; לוודא wrap תקין במובייל.
- i18n: כל טקסט חדש צריך שדה he (ו-en אם הרכיב דו-לשוני).
- אמת: למסגר כהפשטה לימודית (כמו TRUTH_NOTE), לא כצילום מדויק של מנגנון ה-Attention.

### צעדי ולידציה
1. `ROADMAP_STEPS_7` כולל צומת הקשר/Attention בין "וקטורים" ל"דמיון", והמפה נטענת.
2. בדיקה ויזואלית: ChainStrip, ReadingPath וכרטיס שלפני המבדק מזכירים Attention - המושג גלוי לפני המבדק.
3. בדיקת ההדגמה: לחיצה על "הוא" מדגישה את "הכלב".
4. בדיקת התאמת מספור בין מסלול הקריאה למעבדה (אם שונה מספור).
5. הרצת מבדק פרק 8 (5 שאלות) - כל השאלות נשענות על תוכן גלוי.
6. בדיקת היעדר מקף ארוך (U+2014).

---

## סיכום החלטות מנחות
- פרק 1: שינוי מסגור תצוגה בלבד (15 שלבים -> תחנות/מערכות). סיכון נמוך, אין נגיעה ב-quizData/engine.
- פרק 8: תחנת Attention אחת מפורשת + ביט קורפרנס קצר + נקודה בכרטיס שלפני המבדק. אין נגיעה ב-quizData בשלב זה.
- כל 10 יעדי המבחן (שני הפרקים) נשארים תקפים.

סדר מימוש מומלץ: פרק 1 קודם (השינוי הבטוח), ואז פרק 8.
